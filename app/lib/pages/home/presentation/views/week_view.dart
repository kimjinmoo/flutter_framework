import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

// 이주 로또 기록
class Week extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    // 업데이트된 count 변수에 연결
    return Scaffold(
        appBar: AppBar(
          title: const Text("이주 로또번호 히스토리"),
        ),
        body: Center(child: Text("oops"))
    );
  }
}
